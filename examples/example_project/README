Simple example program
====================== 

myProgram calls a function in externalModule.

To compile the program run:
$> make

A detailed compilation procedure it is also included in the compile.sh file.